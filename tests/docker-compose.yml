services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: pgmig
      POSTGRES_PASSWORD: pgmig
      POSTGRES_DB: pgmig
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pgmig"]
      interval: 1s
      timeout: 5s
      retries: 30
